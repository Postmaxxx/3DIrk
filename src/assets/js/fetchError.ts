


/*

const fetchError = async ({f, data}: {data: IFetch, dispatch: ({}: IFetch) => void}) => {

}



export { fetchError }

const result: IErrRes = await response.json()
dispatch(setLoadColors({
    status: 'error', 
    message: result.message, 
    errors: result.errors as TLangText[] || []
}))*/