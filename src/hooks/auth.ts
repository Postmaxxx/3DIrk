
/*const storageName = 'userCredentials'

export const useAuth = () => {


    const login = (token) => {
        localStorage.setItem(storageName, JSON.stringify(token))
    }


    const logout = () => {
        localStorage.removeItem(storageName)
    }




    return { login, logout }
}*/